import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
      listContainer: {
        display: 'flex',
        marginBottom: 30,
        justifyContent: 'flex-start',
      },


}));

